Users:
    - Creating a new user (POST)
    - Updating user info (PUT)
    - Getting user info (GET)
    - Get a list of users that match a query (GET)

Job Postings:
    - Creating a new posting (POST)
    - Closing a posting (POST, updates visibiliy. Should not actually delete it)
    - Updating a posting (PUT)
    - Get posting info by ID (GET)
    - Get a list of job postings that match some sort of query (GET)


